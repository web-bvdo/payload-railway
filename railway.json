{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "NIXPACKS" },
  "deploy": {
    "startCommand": "npm run migrate && node scripts/import-seed.mjs && npm start",
    "restartPolicyType": "ON_FAILURE"
  }
}
