# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# next.js
/.next/
/out/
/build
next-env.d.ts
*.tsbuildinfo

# env (secrets — never commit). Ignore every .env* except the example.
.env
.env.*
!.env.example

# uploads — runtime files on the Railway volume (MEDIA_DIR), not repo assets.
/media

# stray local SQLite (only if someone runs the old adapter) — never commit.
payload.db
payload.db-*

# misc
.DS_Store
*.pem
npm-debug.log*

# editors
/.idea/*
!/.idea/runConfigurations

# vercel
.vercel
